CREATE TABLE PUBLIC.questions (
	id BIGINT NOT NULL AUTO_INCREMENT,
	question VARCHAR_IGNORECASE(255) NOT NULL,
	answer VARCHAR_IGNORECASE(4096) NOT NULL,
	CONSTRAINT QUESTIONS_PK PRIMARY KEY (id)
);
